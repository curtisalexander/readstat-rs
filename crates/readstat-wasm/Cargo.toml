[package]
name = "readstat-wasm"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
readstat = { path = "../readstat", default-features = false, features = ["csv", "ndjson", "parquet", "feather"] }
serde_json = "1"
